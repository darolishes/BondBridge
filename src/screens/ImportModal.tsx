import React from 'react';
import { View, Text } from 'react-native';
import type { ImportModalProps } from '../types/navigation';

const ImportModal: React.FC<ImportModalProps> = () => {
  return (
    <View>
      <Text>Import Modal</Text>
    </View>
  );
};

export default ImportModal;
